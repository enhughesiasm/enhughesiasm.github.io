---
import { ArrowLeftCircle, ArrowRightCircle } from 'iconoir-react';
import type { PostV2 } from '../../components/blog/api/post';

export type Props = {
	post: PostV2;
	icon: 'Right' | 'Left' | 'None';
};
const { post, icon } = Astro.props;
---

<a
	class='flex w-full justify-between space-x-2 text-right leading-tight text-blue-500 hover:text-blue-400'
	href={`${import.meta.env.BASE_URL}articles/${post?.slug}`}
>
	<span
		>{icon === 'Right' && <ArrowRightCircle className='inline !text-lg' />}
		{
			icon === 'Left' && <ArrowLeftCircle className='inline  !text-lg' />
		}</span
	>
	<span><Fragment set:html={post?.title} /></span>
</a>
